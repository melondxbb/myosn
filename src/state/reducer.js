
export const initialState = {
  loading: false
};

const reducer = (state, actions) => {
  switch(actions.type){
    case "":
      return state;
    default: 
      return state;
  };
}

export default reducer;